name: Deploy on Neociies

on: 
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Deploy to Neocities (using neocities-red gem)
      # You may pin to the exact commit or the version.
      # uses: o-200/deploy-sites-neocities-red@5b50ca6f4e26473e19fdf9dcaa0dfbecd3371d10
      uses: o-200/deploy-sites-neocities-red@v1
      with:
        # Neocities API token
        key: 
